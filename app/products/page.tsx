import React from "react";
import Hero from "@/components/hero";
import Store from "@/components/store";

const Products = () => {
	return (
		<section className="paddingX pt-[80px]">
            <Hero />
            <Store />
		</section>
	);
};

export default Products;
